version: '3'
services:
  static_site:
    image: nginx:alpine
    container_name: bkyeni
    ports:
      - "172.16.16.13:8080:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro

    restart: always